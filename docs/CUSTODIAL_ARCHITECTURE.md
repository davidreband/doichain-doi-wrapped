# 🏦 Подробное описание кастодиальной модели WrappedDoichain

## 📋 Обзор архитектуры по модели WBTC

### 🌟 Ключевые принципы:
**Кастодиальная модель** - это система, где **доверенные организации (кастодианы)** физически хранят оригинальные токены DOI на **холодных кошельках**, а в Ethereum сети создаются обеспеченные ими wrapped токены wDOI.

## 🏗️ Архитектура системы

```
┌─────────────────────────────────────────────────────────────────┐
│                    DOICHAIN NETWORK                             │
│  ┌─────────────────┐    ┌─────────────────┐                    │
│  │   Cold Wallet   │    │   Cold Wallet   │                    │
│  │   Custodian 1   │    │   Custodian 2   │                    │
│  │   1000 DOI      │    │   2000 DOI      │                    │
│  └─────────────────┘    └─────────────────┘                    │
└─────────────────────────────────────────────────────────────────┘
                            │
                     Physical Storage
                            │
┌─────────────────────────────────────────────────────────────────┐
│                   ETHEREUM NETWORK                              │
│                                                                 │
│  ┌─────────────────┐    ┌─────────────────┐                    │
│  │    Merchant 1   │    │    Merchant 2   │                    │
│  │  (Initiate)     │    │  (Initiate)     │                    │
│  └─────────────────┘    └─────────────────┘                    │
│           │                       │                            │
│           └───────────┬───────────┘                            │
│                       │                                        │
│          ┌─────────────────────────────┐                       │
│          │  WrappedDoichainCustodial   │                       │
│          │       Smart Contract       │                       │
│          │     3000 wDOI Supply       │                       │
│          └─────────────────────────────┘                       │
│                       │                                        │
│  ┌─────────────────┐  │  ┌─────────────────┐                  │
│  │   Custodian 1   │──┼──│   Custodian 2   │                  │
│  │  (Ethereum)     │  │  │  (Ethereum)     │                  │
│  │   Confirms      │  │  │   Confirms      │                  │
│  └─────────────────┘  │  └─────────────────┘                  │
│                       │                                        │
│  ┌─────────────────┐  │  ┌─────────────────┐                  │
│  │     User 1      │──┼──│     User 2      │                  │
│  │   500 wDOI      │  │  │   800 wDOI      │                  │
│  └─────────────────┘  │  └─────────────────┘                  │
└─────────────────────────────────────────────────────────────────┘
```

## 👥 Основные участники системы

### 🏛️ **1. Кастодианы (Custodians)**
- **Роль**: Хранят DOI токены на холодных кошельках
- **Ответственность**: 
  - Физическая безопасность DOI токенов
  - Подтверждение поступления/отправки DOI
  - Мультисиг подтверждения операций
- **Требования**: Лицензированные финансовые организации

### 🏪 **2. Мерчанты (Merchants)**
- **Роль**: Инициаторы mint/burn операций
- **Функции**:
  - Прием заявок от пользователей
  - Создание запросов на mint/burn
  - KYC/AML проверки пользователей
- **Примеры**: Биржи, обменники, институциональные сервисы

### 👨‍💼 **3. Администраторы**
- **Роль**: Управление системой
- **Полномочия**:
  - Добавление/удаление кастодианов
  - Управление мерчантами
  - Экстренная пауза системы
  - Настройка параметров безопасности

## 🔄 Процесс Mint (Депозит DOI → Создание wDOI)

### Шаг 1: Инициация пользователем
```
Пользователь → Мерчант: "Хочу 100 wDOI"
```

### Шаг 2: Отправка DOI кастодиану
```
Пользователь → Холодный кошелек кастодиана: 100 DOI
```

### Шаг 3: Запрос mint мерчантом
```solidity
// Мерчант создает запрос в контракте
merchant.requestMint(
    userAddress,
    100 * 10^18, // 100 wDOI
    "doichain_tx_hash_abc123",
    "custodian_doichain_address"
);
```

### Шаг 4: Подтверждение кастодианами
```solidity
// Кастодиан 1 подтверждает получение DOI
custodian1.confirmMint(requestId);

// Кастодиан 2 подтверждает (требуется 2 подтверждения)
custodian2.confirmMint(requestId);
// → Автоматически выполняется mint 100 wDOI пользователю
```

## 🔥 Процесс Burn (Сжигание wDOI → Возврат DOI)

### Шаг 1: Запрос на возврат
```solidity
// Мерчант создает запрос на burn
merchant.requestBurn(
    userAddress,
    50 * 10^18, // 50 wDOI
    "user_doichain_address_for_withdrawal"
);
```

### Шаг 2: Подтверждение кастодианами
```solidity
// Кастодианы подтверждают готовность отправить DOI
custodian1.confirmBurn(requestId);
custodian2.confirmBurn(requestId);
// → Автоматически сжигается 50 wDOI
```

### Шаг 3: Отправка DOI пользователю
```
Кастодиан → Doichain адрес пользователя: 50 DOI
```

## 🛡️ Система безопасности

### 🔐 **Мультисиг подтверждения**
```solidity
uint256 public requiredConfirmations = 2; // Настраивается админом

// Каждая операция требует подтверждения от 2+ кастодианов
mapping(uint256 => mapping(address => bool)) public mintConfirmations;
mapping(uint256 => mapping(address => bool)) public burnConfirmations;
```

### ❄️ **Холодное хранение**
- DOI токены хранятся на **оффлайн кошельках**
- Приватные ключи изолированы от интернета
- Мультисиг кошельки для дополнительной защиты

### 📊 **Proof of Reserves**
```solidity
function getReservesInfo() external view returns (
    uint256 totalSupplyAmount,     // Общий объем wDOI
    uint256 totalReservesAmount,   // Общий объем DOI резервов
    bool isFullyBacked            // Полное обеспечение
);
```

### ⏸️ **Экстренная пауза**
```solidity
// Админ может остановить все операции
function pause() external onlyRole(PAUSER_ROLE);
```

## 🏦 Управление кастодианами

### Добавление кастодиана
```solidity
contract.addCustodian(
    "0x1234...",                           // Ethereum адрес
    "BitGo Custodial Services",            // Название организации  
    "DJq9KqHjq5L7MQ8dP4L5V7s6X8zT3nKbVm" // Doichain адрес холодного кошелька
);
```

### Обновление резервов
```solidity
// Обновление количества DOI в резервах кастодиана
contract.updateCustodianReserves(
    custodianAddress,
    1000 * 10^18 // 1000 DOI
);
```

## 📈 Преимущества кастодиальной модели

### ✅ **Безопасность**
- Физическое хранение на холодных кошельках
- Мультисиг подтверждения операций
- Лицензированные кастодианы

### ✅ **Прозрачность**
- Публичные адреса резервов
- Аудируемые операции
- Proof of Reserves в реальном времени

### ✅ **Регулятивная совместимость**
- KYC/AML через мерчантов
- Лицензированные кастодианы
- Соответствие банковским стандартам

### ✅ **Институциональное доверие**
- Проверенные финансовые организации
- Страхование резервов
- Аудит безопасности

## 🔧 Технические детали реализации

### **Роли доступа:**
```solidity
bytes32 public constant CUSTODIAN_ROLE = keccak256("CUSTODIAN_ROLE");
bytes32 public constant MERCHANT_ROLE = keccak256("MERCHANT_ROLE");  
bytes32 public constant PAUSER_ROLE = keccak256("PAUSER_ROLE");
bytes32 public constant RESERVE_MANAGER_ROLE = keccak256("RESERVE_MANAGER_ROLE");
```

### **Структуры данных:**
```solidity
struct MintRequest {
    address recipient;          // Получатель wDOI
    uint256 amount;            // Количество для mint
    string doichainTxHash;     // Hash транзакции DOI
    string custodianAddress;   // Адрес кастодиана
    address merchant;          // Инициатор запроса
    uint256 timestamp;         // Время создания
    bool approved;             // Статус одобрения
    bool executed;             // Статус выполнения
    uint256 confirmations;     // Количество подтверждений
}
```

### **События для аудита:**
```solidity
event MintRequested(uint256 indexed requestId, address recipient, uint256 amount);
event MintConfirmed(uint256 indexed requestId, address custodian, uint256 confirmations);
event MintExecuted(uint256 indexed requestId, address recipient, uint256 amount);

event BurnRequested(uint256 indexed requestId, address account, uint256 amount);
event BurnConfirmed(uint256 indexed requestId, address custodian, uint256 confirmations);
event BurnExecuted(uint256 indexed requestId, address account, uint256 amount);
```

## 🧪 Тестирование

**21 комплексный тест** покрывающий:
- ✅ Управление кастодианами
- ✅ Управление мерчантами  
- ✅ Процесс mint с мультисиг
- ✅ Процесс burn с мультисиг
- ✅ Proof of Reserves
- ✅ Административные функции
- ✅ Безопасность и паузы

## 🚀 Готовность к продакшну

Кастодиальная модель готова для:
- **Институционального использования**
- **Регулируемой среды** 
- **Масштабируемых операций**
- **Аудируемой прозрачности**

Эта архитектура обеспечивает максимальную безопасность и доверие, следуя проверенной модели WBTC! 🏦🔐

## 📁 Файлы проекта

### Контракты
- `contracts/WrappedDoichainCustodial.sol` - Основной кастодиальный контракт
- `contracts/WrappedDoichain.sol` - Базовый bridge контракт

### Тесты  
- `test/WrappedDoichainCustodial.test.js` - 21 тест кастодиальной модели
- `test/WrappedDoichain.test.js` - 28 тестов базовой модели

### Скрипты
- `scripts/deploy.js` - Деплой контрактов
- `scripts/manage-bridges.js` - Управление мостами

### Документация
- `README.md` - Основная документация
- `CLAUDE.md` - Техническая документация для разработчиков
- `docs/CUSTODIAL_ARCHITECTURE.md` - Данный файл с описанием архитектуры